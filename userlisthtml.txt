<div class="page-header">
  <h1>Manage Users</h1>
</div>

<div class="alert" [class.alert-success]="messageType==='success'" [class.alert-error]="messageType==='error'" *ngIf="message">{{ message }}</div>

<app-loading-spinner *ngIf="isLoading"></app-loading-spinner>

<table class="data-table" *ngIf="!isLoading">
  <thead>
    <tr><th>Name</th><th>Username</th><th>Email</th><th>Role</th><th>Books</th><th>Fines</th><th>Status</th><th>Action</th></tr>
  </thead>
  <tbody>
    <tr *ngFor="let user of users; trackBy: trackByUserId" [class.inactive-row]="!user.isActive">
      <td>{{ user.fullName }}</td>
      <td><code>{{ user.username }}</code></td>
      <td>{{ user.email }}</td>
      <td>
        <span class="role-badge" [class.role-librarian]="user.role === 'librarian'">{{ user.role }}</span>
      </td>
      <td>{{ user.currentBooksCount }} / {{ user.maxBooksAllowed }}</td>
      <td [class.has-fines]="user.totalFines > 0">{{ user.totalFines | currency }}</td>
      <td>
        <span class="status-dot" [class.active]="user.isActive" [class.inactive]="!user.isActive">
          {{ user.isActive ? 'Active' : 'Inactive' }}
        </span>
      </td>
      <td>
        <button class="btn-toggle"
          [class.btn-deactivate]="user.isActive"
          [class.btn-activate]="!user.isActive"
          (click)="toggleActive(user)">
          {{ user.isActive ? 'Deactivate' : 'Activate' }}
        </button>
      </td>
    </tr>
  </tbody>
</table>