<div class="form-card">
  <h3 class="form-title">{{ isEditing ? 'Edit Book' : 'Add New Book' }}</h3>

  <form [formGroup]="bookForm" (ngSubmit)="onSubmit()" novalidate>
    <div class="form-row">
      <div class="form-group">
        <label>Title <span class="required">*</span></label>
        <input formControlName="title" class="form-control" [class.is-invalid]="getError('title','required')" placeholder="Book title" />
        <div class="field-error" *ngIf="getError('title','required')">Title is required.</div>
      </div>
      <div class="form-group">
        <label>Category</label>
        <input formControlName="category" class="form-control" placeholder="e.g. Fiction" />
      </div>
    </div>

    <div class="form-group">
      <label>Description <span class="required">*</span></label>
      <textarea formControlName="body" rows="3" class="form-control"
        [class.is-invalid]="getError('body','required')" placeholder="Book description"></textarea>
      <div class="field-error" *ngIf="getError('body','required')">Description is required.</div>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label>ISBN</label>
        <input formControlName="isbn" class="form-control" placeholder="978-..." />
      </div>
      <div class="form-group">
        <label>Published Year</label>
        <input formControlName="publishedYear" type="number" class="form-control" placeholder="e.g. 1984" />
      </div>
      <div class="form-group">
        <label>Total Copies <span class="required">*</span></label>
        <input formControlName="totalCopies" type="number" class="form-control"
          [class.is-invalid]="getError('totalCopies','min') || getError('totalCopies','required')" />
        <div class="field-error" *ngIf="getError('totalCopies','min') || getError('totalCopies','required')">
          At least 1 copy required.
        </div>
      </div>
    </div>

    <div class="form-actions">
      <button type="button" class="btn-cancel" (click)="formCancel.emit()">Cancel</button>
      <button type="submit" class="btn-submit" [disabled]="bookForm.invalid">
        {{ isEditing ? 'Save Changes' : 'Add Book' }}
      </button>
    </div>
  </form>
</div>